.row
  .col-md-6
    %h1 Oś Nastroju
  .col-md-6
    %br
    = link_to 'Dodaj nowy wpis', new_moodentry_path, class: "btn btn-large btn-primary pull-right"


= line_chart Moodentry.where(user_id: current_user).group(:date).average(:mood)

%table.table
  %tr
    %th Data
    %th Notatka
    %th Nastrój
    %th
    %th

  - moodentries.where(user_id: current_user).each do |moodentry|
    %tr
      %td= moodentry.date
      %td= moodentry.note
      %td= moodentry.mood
      %td= link_to 'Edytuj', edit_moodentry_path(moodentry)
      %td= link_to 'Usuń', moodentry_path(moodentry), :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'Dodaj nowy wpis', new_moodentry_path, class: "btn btn-large btn-primary pull-right"

