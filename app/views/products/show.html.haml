%ol.breadcrumb
  %li
    %a{href: root_path} Categories
  %li
    %a{href: category_path(category)} #{category.name}
  %li.active #{product.title}

%h2 #{product.title} #{link_to raw('<span class="glyphicon glyphicon-pencil small"></span>'), edit_category_product_path(category.id, product.id)}

.row
  .col-xs-6.col-md-3
    .thumbnail
      = link_to product.picture, rel: :lightbox do
        = image_tag product.picture, height: 100  
  .col-xs-6.col-md-3
    .thumbnail
      = link_to product.picture, rel: :lightbox do
        = image_tag product.picture, height: 100 
%p
  %b 
  = product.description
%p
  %b Breed:
  = product.breed
  %p
  %b Gender:
  = product.gender
  %p
  %b Year of Birth:
  = product.YoB
  %p
  %b Height:
  = product.height
  %p
  %b color:
  = product.color
  %p
  %b Price:
  = product.price

%p
  %b Category:
  = product.category.name
%p

  %iframe{:src => product.youtube, height: "400", :width => "600"}

= link_to 'Back', category_path(category)


- if user_signed_in?
  %p 
  = link_to "add a new picture", new_picture_path, class: "btn btn-large btn-primary"
